CREATE TABLE IF NOT EXISTS learning (
    key           TEXT    NOT NULL,
    source_locale TEXT    NOT NULL,
    target_locale TEXT    NOT NULL,
    grade         INTEGER NOT NULL,
    type          TEXT    NOT NULL,
    interval_days INTEGER NOT NULL DEFAULT 0,
    next_review   INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (key, source_locale, target_locale, type)
);

upsertGrade:
INSERT OR REPLACE INTO learning (key, source_locale, target_locale, grade, type, interval_days, next_review)
VALUES (:key, :sourceLocale, :targetLocale, :grade, :type, :intervalDays, :nextReview);

getGrade:
SELECT grade, interval_days FROM learning
WHERE key = :key AND source_locale = :sourceLocale AND target_locale = :targetLocale AND type = :type;

countDue:
SELECT COUNT(*) FROM learning
WHERE source_locale = :sourceLocale AND target_locale = :targetLocale AND type = :type AND next_review <= :today;

getKeysDue:
SELECT key FROM learning
WHERE source_locale = :sourceLocale AND target_locale = :targetLocale AND type = :type AND next_review <= :today;

getGradesByDirection:
SELECT key, grade FROM learning
WHERE source_locale = :sourceLocale AND target_locale = :targetLocale AND type = :type;

getAllDue:
SELECT key, grade FROM learning
WHERE source_locale = :sourceLocale AND target_locale = :targetLocale AND type = :type AND next_review <= :today;
